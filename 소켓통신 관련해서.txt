Buffer : 저장공간 임시 공간(담아내는 공간)
Stream : A와 B가 서로통신을 할 때 선이 필요한대 그게 Stream
==================================================
포트 : 어떤 프로그램을 전송해야할지 분명하게 하는것
소켓통신 
소켓은 포트 통신이다!
양끝단에 포트를 통해 ByteStream에 의해서 통신을 하는것
양끝단 포트를 두고 통신

소버소켓 ---IP주소---- 클라이언트 소켓

ByteStream이 연결이 되려면 
1. 서버소켓(main)을 실행해야됨
2. ip주소를 연결시켜야됨 
서버소켓 : 연결만 받고 통신은 못함!!

서버소켓:1000 실행시 ->
 클라이언트 소켓이 실행되면서 ->
 서버소켓에게 ip주소:포트번호1000으로 연결시도함

서버소켓의 역할 : 연결만 받아 새 소켓을 만들어 클라이언트 소켓이랑 연결된다
그 새로운 소켓은 포트번호를 랜덤으로 선정한다
그리고 클라이언트 소켓과 실제 ByteStream으로 연결된다

ServerSocket(포트번호) : 외부에서 악성코드나 해커등 방해 요소들이 들어올 수 있으니까 포트번호가 이게 맞는지
물어보는거임
socket - serverSocket.accept(); // 클라이언트 접속 대기중 (연결이 시도될 때 까지)

소켓(br, Input)과 클라이언트 소켓에 연결단 ByteStream이 Socket.getInputStream() 이다
소켓은 클라이언트가 출력한것을 읽어야하니까 br.readLine()